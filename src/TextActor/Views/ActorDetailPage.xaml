<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TextActor.Views.ActorDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:TextActor.ViewModels"
    x:DataType="vm:ActorDetailViewModel">
    <ContentPage.BindingContext>
        <vm:ActorDetailViewModel />
    </ContentPage.BindingContext>
    <Grid>
        <StackLayout Padding="15" Spacing="20">
            <Label FontSize="Medium" Text="Name:" />
            <Label FontSize="Small" Text="{Binding Name}" />
            <Label FontSize="Medium" Text="Pitch:" />
            <Label FontSize="Small" Text="{Binding Pitch}" />
            <Label FontSize="Medium" Text="Volume:" />
            <Label FontSize="Small" Text="{Binding Volume}" />
            <Button Command="{Binding PlayCommand}" Text="Test" />
        </StackLayout>
    </Grid>
</ContentPage>